
// ! Notacion

// ; -> Delimitar el final de una linea, no es obligatorio

const b = 4;
b + 4;

const a = 4
a + 4

// . -> Se utiliza en los objetos para acceder a los atributos

movil.altura;


// [] -> Se utilizan para las listas, arreglos o arrays

const ar = [1, 2, 3, 4]
console.log(ar[2]);

// () -> Se utilizan para las funciones

function suma(a, b) {
    // Se escribe la funcion
}


// {} -> Llaves para objetos, funciones y estructuras de control
const movil = {
    anchura: 5,
    altura: 10
}

if (true) {
    // todo lo que haya entre llaves
}

